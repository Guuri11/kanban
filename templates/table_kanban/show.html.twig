{% extends 'base.html.twig' %}

{% block title %}TableKanban{% endblock %}

{% block body %}
    <h1>TableKanban</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ table_kanban.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ table_kanban.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('table_kanban_index') }}">back to list</a>

    <a href="{{ path('table_kanban_edit', {'id': table_kanban.id}) }}">edit</a>

    {{ include('table_kanban/_delete_form.html.twig') }}
{% endblock %}
