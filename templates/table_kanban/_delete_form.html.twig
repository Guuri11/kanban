<form method="post" action="{{ path('table_kanban_delete', {'id': table_kanban.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ table_kanban.id) }}">
    <button class="btn">Delete</button>
</form>
