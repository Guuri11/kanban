{% extends 'base.html.twig' %}

{% block title %}ColumnKanban{% endblock %}

{% block body %}
    <h1>ColumnKanban</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ column_kanban.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ column_kanban.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('column_kanban_index') }}">back to list</a>

    <a href="{{ path('column_kanban_edit', {'id': column_kanban.id}) }}">edit</a>

    {{ include('column_kanban/_delete_form.html.twig') }}
{% endblock %}
